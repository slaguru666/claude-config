---
type: log
month: 2026-09
---

# Log — September 2026

Append-only. Newest at the bottom. Corrections are new entries, never edits.

- **2026-09-07** — Masque: Tim rejected the multi-panel app as not what he
  wanted; built the minimal `voice.html` and then `studio.html` with 65 voice
  presets. Three usable pages now coexist. [[masque]]
- **2026-09-08** — Five audio apps published to the public game site as the
  SOUND suite. Local ports remain the place to edit; deploy is a file copy.
  [[oneoffgames-vps]]
- **2026-09-09** — RingBRP design corpus made public as `ringbrp-design`;
  scanned books and extracted page images held back by `.gitignore` (4 MB
  published instead of a gigabyte). [[custodians-ringbrp]]
- **2026-09-09** — Confirmed `rules.mjs` is the sole rule authority and has
  superseded four rulings still carried in the `design/` specs. Read it before
  quoting any rule. [[custodians-ringbrp]]
- **2026-09-09** — Loom gained a Gitea remote; earlier notes saying history
  could be rewritten freely no longer apply. [[loom]]
- **2026-09-11** — AFTERIMAGE desk-playtested in full from the GM chair at
  v3.1: 2:47 on the clock inside the 3-hour ceiling. Fifteen findings, all
  applied in v3.2. [[afterimage]]
- **2026-09-12** — Corkboard Phase 2 settled: dual schema definitions with
  conformance testing, and HTML markup under a narrow allow-list with a custom
  editor. [[corkboard]]
- **2026-09-12** — Governing rule adopted for the portable schema: **never
  stricter than Foundry.** A validator that refuses what Foundry accepts is a
  bug, not safety. [[corkboard]]
- **2026-09-12** — Corkboard Phase 2 complete: portable schema, validator and
  conformance testing agree with Foundry v14.363 on zero differences; 349
  tests green; committed `3ee09e1` and pushed. [[corkboard]]
- **2026-09-12** — Two interactive sessions raised a false alarm at each other
  across `~/Git/corkboard`; the lessons about shared working trees went into
  [[CONVENTIONS]].
- **2026-09-13** — Decided to keep a durable Markdown record of all this work
  in the iCloud Obsidian vault, because claude-mem, Graphiti and the per-machine
  memory files are all losable and none of them are Tim's. Design in [[DESIGN]].
- **2026-09-13** — Vault built: `Ai/Claude/` with 27 project notes, [[INDEX]],
  [[TASKS]], [[CONVENTIONS]] and this log, seeded from the existing memory files.
  Wired into `~/.claude/CLAUDE.md` so every session on every Mac reads and updates it.
- **2026-09-13** — Vault moved: **`~/Vault/slavault/Ai/Claude/` is now canonical**,
  because that is the vault Tim actually works in. The iCloud vault becomes a one-way
  mirror for reading tasks on the phone, and must not be edited.
- **2026-09-13** — MINI-S access solved without Gitea. The vault rides in the
  `claude-config` repo as `vault/`, which MINI-S already pulls from GitHub over SSH —
  no new repo, no new keys, none of the manual Gitea web-UI steps a separate mirror
  would have needed. Git is now the durable layer, so iCloud is no longer load-bearing.
  [[mini-s]] · [[claude-config-sync]]
- **2026-09-13** — Correction: **MINI-S is reachable from the Mac Mini.** The note
  claiming different subnets blocked LAN SSH is wrong — ping and key-only SSH from
  192.168.0.30 to 192.168.1.6 both verified working. [[mini-s]]
- **2026-09-13** — Correction: **Claude Code is not installed on MINI-S.** `~/.claude`
  carries history and sessions so it ran once, but there is no `claude` binary and no
  Node or npm on the box. The vault and config are staged there correctly and will work
  on reinstall; Node needs sudo, so that step is Tim's. [[mini-s]]
- **2026-09-13** — Fixed a long-standing bug in `install.sh`: the Obsidian MCP vault
  candidate was `~/Vault/slavault/slavault`, a path that cannot exist, so that step
  silently skipped on every Mac. Now `~/Vault/slavault`. [[claude-config-sync]]
- **2026-09-13** — MINI-S toolchain rebuilt **without sudo**: Node v24.21.0 from the
  official tarball (sha256 verified) into `~/.local`, npm global prefix pointed at
  `~/.local/npm-global` so a Node upgrade cannot orphan the globals, then Claude Code
  2.1.270, Codex CLI and QuickDesign. MCP servers registered and connecting, including
  Graphiti and Playwright. The box is usable again. [[mini-s]]
- **2026-09-13** — Learned the hard way: PATH belongs in `~/.profile` on Ubuntu, not at
  the bottom of `~/.bashrc`, which returns early for non-interactive shells and so hides
  the export from ssh commands, scripts and `bash -lc`. [[mini-s]]
- **2026-09-13** — **Security:** two GitHub personal access tokens are stored in
  plaintext at the end of MINI-S `~/.bashrc`, and were surfaced into a session
  transcript by a routine recon command. Both need rotating; values deliberately not
  recorded here. [[mini-s]]
- **2026-09-13** — Cleaned the GitHub tokens off MINI-S's readable files. They were
  exported from both `.bashrc` (644) and `.zshrc` (664), and one was embedded in a
  stale `settings.local.json` permission allow-rule (664) left over from a session that
  had echoed it. Moved to `~/.config/github/tokens.env` (600) sourced from `.profile`
  and `.zshrc`; the allow-rule deleted; `settings.local.json` tightened to 600. Verified
  zero group- or world-readable copies remain. **Rotation is still outstanding** — the
  values exist in `history.jsonl` and several `.claude.json` backups, and cleaning a
  leaked secret off disk is not the same as revoking it. [[mini-s]]
- **2026-09-13** — Corkboard Phase 3 viewport and gesture arbitration landed. Every
  element drag now rebases on concurrent edits, cards and zones included (Tim's call):
  a drag means "from where this is now", so another player's move is never silently
  undone. Five Codex review rounds found **six defects**, all fixed and each verified
  live. The sharpest: a drag with a floor must rebase from the **pointer**, never from
  the clamped preview — carrying the clamped difference made the committed size depend
  on whether one more `pointermove` happened to arrive (320 or 200 for the same pointer
  position). All eight `reacquire` implementations now have a live pass. Head `cde4cff`.
  [[corkboard]]
- **2026-09-13** — Three of those six defects were in work written in that same session,
  including a "regression test" that guarded nothing — no test can import `sheet.mjs`,
  which imports Foundry. The fix each time was to move state into a testable module
  rather than write more tests around the untestable part. Recorded as a working
  pattern, not a one-off. [[corkboard]]
- **2026-09-13** — `codex --profile deep` had been failing for weeks on the Mac. Both
  `codex-ops-kit` clones were stale and the fixed installer had never run; worse, the
  kit's managed root keys had been landing **inside a `[projects.*]` table** and doing
  nothing, which masked that its pinned model had gone stale. Installing the fix made
  the stale name live and broke `codex` outright — only `gpt-6-astra` and `gpt-5.5`
  still answer for a ChatGPT account. Profiles now per-file, models corrected, both
  clones synced. Plain `codex` now runs at medium effort where the app had left it at
  low. [[codex]]
- **2026-09-13** — Foundry was left with **no active world** for ~2h after a world
  switch: shutting Vaesen down landed on `/auth`, which needs the admin password, and
  entering one is off-limits. Tim launched Blade Runner himself. Restoring a world
  without the password means `"world": "<id>"` in `/root/foundrydata/Config/options.json`
  plus a service restart — a production edit worth asking about first. [[oneoffgames-vps]]
- **2026-09-13** — Corkboard card editor showed **white text on a white background**
  when typing a note body. Two fields pinned a light background and left the colour to
  inherit — under Foundry's dark theme that is near-white. Fixed by pinning `#241c14`
  alongside the paper, including on ProseMirror's own `.editor-content`, which the
  host's colour does not reach. Measured 16.49:1 after deploying. The general rule,
  worth applying across this stylesheet: **a pinned background needs a pinned colour**,
  or the theme decides half the pair. `6add329`. [[corkboard]]

- 2026-09-13 — Corkboard: a line's minimum size is measured between its stored
  endpoints, not across its box. The first guard used the box diagonal, which is
  the line's length only while the ends sit in opposite corners; one shift+Down
  gave the box a side the line never had and the guard let the width go to zero.
  Found by Codex review 21, reproduced, fixed, verified live on `cb2` (`d9fc738`).
  Two existing tests had been passing for the wrong reason — they claimed to
  measure diagonals using a horizontal fixture.
- 2026-09-13 — Corkboard: Phase 3 gesture extraction complete. Cards/strings,
  shapes/ink and zones each in their own module behind a `BoardHost` seam;
  `sheet.mjs` down ~412 lines, 614 tests.
- 2026-09-13 — Corkboard: `addShape` measured its rounded box, not the drag, so
  a 13.5-unit drag was accepted into a 14 box while the line inside stayed 13.5
  — drawable at a length it could not be resized to. Both guards now ask one
  `lineLength()`. Codex review 22; `d3ee375`. The lesson is that two guards on
  the same property each measuring a convenient proxy will disagree, and the
  disagreement is invisible until someone asks them the same question.
- 2026-09-13 — RingBRP: nine guard defects in one session, all the same shape — a check
  that passes for a reason unrelated to what it claims. The bestiary contradicted a wired
  power under eighteen green guards; a scope anchored on a moved name silently became the
  whole file (3,184 chars to 30,825, test still green); `<!-- cast : -->` and `[cus: ]`
  were invisible to their readers, so a guard fell back to the whole duty roster and
  reported OK; and playtest records were being checked as scenarios with live rolls.
  R-282..R-293, 18 guards to 19. The house pattern is now: widen the reader for the
  spellings you thought of, and add a looser counter that names the one you did not.
- 2026-09-13 — Corkboard: live Foundry 14.363 does **not** enforce a
  `TypedObjectField` member schema's min/max on `page.update()` — an out-of-range
  shape endpoint is stored, survives `toObject` and a full reload, and the
  member's defaults are never filled in. `validateBoard` (import, standalone app)
  does enforce it. A reviewer asserted the opposite from testing the cleaner in
  isolation; checking it live changed the reason for the fix, not the fix.
  Codex review 23; `4911529`.
- 2026-09-13 — **Correction to the entry above.** That probe was invalid: it ran
  against a module whose imports were 404ing because I deployed only the files I
  had edited, so the `corkboard.board` subtype never registered and there was no
  data model to clean anything. Foundry 14.363 **does** clamp, and fills the
  defaults. The missing defaults were the tell. Two lessons kept: deploy the
  build rather than the files you touched, and a claim of "equivalent mutant" is
  a claim about all inputs — enumerate it, never reason it. Codex review 24;
  `b7947f6`.

### 13 September — CLEAN GROUND v0.15 → v0.20.1, five desk passes, two new guards

Nine desk playtests total now. Passes 5–9 ran and their fix lists were applied; passes 8
and 9 were new shapes — a **cold read** (the document as a GM meeting it 30 minutes before
a slot) and a **regression pass** (every fix from passes 4–8 re-checked). Both found things
dice never had: a numbered list running 1,2,3,4,6,5 under a heading saying "five", and a
claim that had been wrong by a factor of three since v0.15.

Two guards added. `check-outcomes` (R-290) reads every bracketed mechanic tag in the acts
and holds what its beat states — the document covered failure on 16 of 21 beats and a
special on 3, against 85% of sessions hitting a special with nothing written.
`check-packs` (R-296) measures EXPOSURE's fight tables into an artifact, and building it
found every published figure had been measured at `runFight`'s default 40-round ceiling:
the four-player hollow-man wipe rate was **58.7% and is 74.9%**, with 578 of 2000 runs never
finishing.

Worked alongside a second Claude session throughout — disjoint files, claims verified rather
than exchanged. Four readers were caught this session unable to see their own format (cast
declarations, beat tags, citation markers, the `POWER:` marker), and the fix that keeps
paying is an **asymmetric counter**: something that reports what the strict reader could not
parse. Both of us wrote a test that passed for a bad reason; mine passed on the exact thing
it was written to catch, which is the harder one to see.

CLEAN GROUND is convention-ready but for a run with human beings. `9c2ce9d`, `a5c4378`,
`137ab82`.

## 2026-09-13 — the standalone Corkboard runs

Slice one of the standalone app is built and verified live: a browser page with
no Foundry that creates, names, opens and deletes boards; adds cards and zones;
drags, resizes, ties string, draws and rubs out; and reopens a board with
everything still there. Not a second corkboard — the renderer, the stylesheet,
every gesture and all the arithmetic are the module's own files, reached through
the `BoardHost` seam. 781 tests. Head `5246a85`, pushed. Record in
`docs/verification/2026-09-13-standalone-slice-1.md`.

- **The multi-tab lock is `navigator.locks`, not a `BroadcastChannel` election.**
  Contracts §5 amended in place. A BroadcastChannel post from `pagehide` is
  queued as a task and the document is destroyed before it runs, so closing the
  tab holding a board left the other read-only for good, and nothing in an
  announce-and-listen protocol can hear a tab that died without speaking. The
  browser owns its lock and releases it when the tab goes. Every decision in §5
  stands; only the mechanism moved.
- **A change is shown only after it is stored.** Showing first is faster by one
  IndexedDB round trip and lies whenever the write fails — which on a device
  whose storage can be evicted is the expensive option.
- **Writes are declared as operation lists rather than handed-out transactions**,
  which makes "the board and its commit land together" structural and sidesteps
  a transaction held across an `await` being closed by the browser.
- **The gesture arbitration order lives in one place** now that there are two
  hosts. Two literals agree only while somebody remembers they must.
- Live verification found six defects. Five were mine and are fixed. The sixth —
  a pen tap storing a one-point stroke that can never be seen, selected or
  removed — is in shared code, is in the module today, and needs its own
  decision, so it is recorded and spawned as its own task.
- Another session was committing to this repo at the same time. The files were
  disjoint and nothing was lost, but `git add -A` was luck rather than discipline.
- 2026-09-13 — RingBRP, later the same day: CLEAN GROUND's published figures are now held
  in both directions. `check-cited` gained **derived** sources — enumerated from `rules.mjs`
  on every build, nothing stored, so there is no `--update` able to silence a disagreement —
  and 32 markers went onto the step-5 prose rather than only its tables, because all three
  figures that had gone bad were in sentences. A peer's pack-table baseline found the
  published wipe rate was 58.7% where the truth is 74.9%: every fight figure had been
  measured at a 40-round default cap that `runFight` does not report. R-294..R-297, 19 guards
  to 20.

- **2026-09-13 — CLEAN GROUND v0.22; desk pass 11 closes the last branch the desk can reach.**
  The fight the players choose, played: a median 21 rounds and 0.97 deaths against an act
  budgeted at fifty. Six findings, all applied. The one that matters: two blocks two hundred
  lines apart specified two different fights for the same trigger — EXPOSURE's "use three"
  (0.1% wiped) against the mixed force the trigger actually produces (8.3%) — both written by
  me, the mild one predating the measurement. Also written: the count moves after a fight and
  the almanac handout becomes a record of what they did without a word from the GM; Ivy is
  ruled out of the target pool because the Close is hers; a dead PC gets the next name on the
  duty roster, arriving knowing nothing, which buys a recap. Applying it turned up a stale
  "median 24 rounds" in two places that no guard could see, because a bare figure with no
  citation marker is invisible to `check-cited`. Concurrently, session b5 found the anchor
  block assumes cooperation and never answers the party that shoots; I re-derived their 4.90%
  exactly and shipped their unchecked premise with it, corrected within the hour — the
  scenario has no Transposition roll at all, so it was a right number with no question
  attached. R-298. Eleven desk passes; only a human run left.

## 2026-09-13 — one sanitiser, two hosts, and a note you can write

Slice 2 of the standalone Corkboard. A note body is now decided in one place,
`src/data/sanitize.mjs`, and both hosts use it. Head `3603140`, pushed. Record
in `docs/verification/2026-09-13-standalone-slice-2.md`.

- **Two tiers, not one.** `ingest` says what may be stored, `display` says what
  is drawn, and they are deliberately different sets — a table written in
  Foundry is legal but undrawn, so it survives a trip through the app instead of
  being quietly deleted by it. Conflating them is what would make a
  Foundry → iPad → Foundry round trip lossy.
- **Tokenise and rebuild, never strip.** Both tiers parse the input and write a
  fresh document from the parse with everything escaped. A sanitiser that edits
  its input hands the browser bytes it never fully parsed, and the gap between
  what it thought it read and what the parser decides those bytes mean is where
  the whole mutation-XSS genre lives.
- **Both hosts moved off `foundry.utils.cleanHTML`.** A source-level guard in
  the suite keeps it that way. The display tier is narrower than core's — tables
  and images in a Foundry note now draw as text — which is the contract's call,
  not a regression: what is drawn is exactly what the editor can offer back.
- **The Foundry save path is not verified live.** One line in `sheet.mjs`, no
  unit tests on that file and never has been, and no deploy this slice. It is
  owed a run inside Foundry.
- Six defects found live, all fixed, including one left behind by slice 1:
  `TAP_SLOP` was referenced and never declared, so tapping bare cork to clear
  the selection threw inside a `pointerup` listener and had never once worked.
  The mutation sweep — not hand-testing — found two of the others, both ways of
  smuggling a CSS `url()` past the check.
- The lesson that keeps repeating: a decision inline in a DOM handler is a
  decision no test can reach. Lifting `isTap` and `bodyContent` out is what
  turned two survivors of the sweep into caught mutations.
- 2026-09-13 — Corkboard: the undo-collapse guard is gone. Three rules in three
  review rounds each refused a safe undo, because `undoConflict` cannot tell a
  genuine restore from a recombination. The real fault was that the undo witness
  watched only the places the payload wrote — a resize watches w and h, so an
  endpoint swung round by someone else was invisible. `readPlaces` now watches a
  shape's box and ends as one coupled set and the existing drift check does the
  work. `0abdd4d`. **Unreviewed** — Codex hit its account usage limit (resets
  19:59); re-run review 25 when it is back.

- **2026-09-13 — `check-figures`, the 21st guard: figures with nothing holding them.**
  `check-cited` can only resolve the markers that exist, so a measured number printed with no
  marker beside it was never looked at again — which is how "a median 24 rounds" survived every
  pass against a baseline holding 21. The new guard reads the figures instead of the markers,
  the fifth asymmetric counter in the repo and the first running opposite to the reader it
  covers. Narrow on purpose: a first draft matching any number near a measurement word found
  282 candidates, nearly all ordinary prose. Documents that cite nothing are held by a ratchet
  rather than failed, and join the strict regime on their first marker. It found 74.9% printed
  bare twice — the figure that was published at 58.7% until the truncated sweep — and "the
  longest fight is 120 rounds", where 120 is exactly the field `check-cited` refuses to let
  anybody cite: two rules each right, and a hole between them. Proved by breaking it four ways,
  every restore byte-identical; the false positive in the first draft was a table header, fixed
  by testing context rather than the match. CLEAN GROUND v0.23, R-299.

- **2026-09-13 — Corkboard: a tap with the pen leaves no mark.** Defect 6 from the
  standalone slice-1 live run, left alone at the time because it needed a decision
  rather than a patch. A press with no travel stored a stroke of one point, and a
  `<polyline>` with one point has no subpath: it painted nothing, measured 0x0, and
  its `.cb-stroke-hit` companion inherited the same empty box, so the mark could be
  counted but never seen, selected or rubbed out. Decided **refuse**, not draw. A
  tap is how you reach a mark with the pen still in your hand — the app selects on
  the press, the module opens a mark's menu on a right-click — so a tap that painted
  would stamp ink on the very thing you were reaching for; and the highlighter's
  chisel tip is a `butt` linecap, which paints nothing for a zero-length subpath, so
  repeating the point would have left a highlighter tap exactly as invisible as it
  already was. The rule is one predicate, `isStroke`, asked by both `addStroke` and
  the renderer. Two things worth keeping: the comment arguing "a dot still deserves
  to exist" sat on a branch that could never run — `thinStroke` keeps its first point
  regardless — so the dot was never a decision anyone took; and refusing does not
  retract the one-point strokes already on saved boards, which `transfer.mjs` copies
  between worlds wholesale, so the renderer now declines to draw them instead of
  emitting a polyline that lies about being there. 12 of 12 mutants caught, 968 → 976
  tests. Committed `ab20565` alongside a concurrent session's in-flight asset work,
  staged hunk by hunk so none of theirs went in.


## 2026-09-13 — pictures, stored once

Slice 3 of the standalone Corkboard. A picture is stored under `asset:` and the
SHA-256 of its bytes, and the URL a browser can load is made at draw time and
revoked when the screen goes. Head `69238ab`, pushed, 1090 tests. Record in
`docs/verification/2026-09-13-standalone-slice-3.md`.

- **Resolution is a seam, not a step.** `mountBoard` and `applySurface` take a
  resolver; a host that supplies none is one whose references are already URLs,
  which is Foundry. A `blob:` URL is valid only for the document that made it, so
  one in a saved board is a dangling reference the moment the page reloads.
- **Reclaiming scans the snapshots AND the commits.** A board is rebuilt by
  replaying its log, so bytes only an old commit names are bytes a replay will
  ask for. The practical effect is that a picture goes when the board goes, not
  when the card does — which is the right way round, because removing a card is
  something people undo.
- **The sweep was blind to every board on the device, and the tests agreed with
  it.** It read a field off the `boards` row that does not exist — that row is
  metadata; a board's content is a snapshot plus the commits after it — and the
  fixtures had invented the same shape. Found by dumping real IndexedDB records
  during the live run. The faithful storage fake now lives in
  `test/helpers/fake-idb.mjs` so there is one of it, and fixtures build boards
  through `BoardStore` rather than by hand.
- Tim asked whether this could become a shared server app. Answered: it is
  already phases 5–6 of the design, the commit is already the transaction, and
  the real missing piece is per-entity preconditions — not the socket.

- **2026-09-13 — CLEAN GROUND artwork sheet written; `check-figures` taught table headers.**
  The scenario had no NPC portraits, no scene plates and no map, while LAST_ADMISSION has 8
  plates, OPEN_DAY 10 and THROUGH_TRAIN 13 — and the Casting section said "no new art needed",
  which was only ever true of the cast. `CLEAN_GROUND_ART.md` now carries 6 NPC portraits and
  11 scene plates; art direction draws everything as a sheet from the 1962 file including the
  far side, because the valley is a filed document rather than a place. Nothing generated yet;
  it is on Still to do. Separately, the concurrent session's R-300 showed `check-figures` green
  on a bare **24** in a table headed "Median rounds" — a vocabulary learned from marked figures
  cannot contain the phrasing of the figure nobody marked. Repaired by having a cell inherit
  its column's header, which the document states rather than the guard guessing. Two bugs in
  the repair caught before commit, the worse one reporting ~130 correctly cited rows as broken.
  Coverage 25 figures to 120. v0.24, 22 guards, R-301.

- **2026-09-13 — Corkboard review 26: the escape hatch I closed without seeing it.**
  Codex (deep, gpt-6-astra, after the 19:59 quota reset that had also blocked review
  25) found **no production defect** in `ab20565`. It reviewed an isolated snapshot
  rather than the working tree, which mattered — another session was editing the same
  files and a disk-reading review would have blamed me for their half-built asset
  seam. Two findings. First, every valid fixture in my new renderer test used exactly
  **two** points, so a guard of "exactly two points" and an `isStroke` demanding an
  even length both survived — two mutants my own twelve never tried, because I built
  the fixtures and the sweep from the same assumption. Closed; 6 of 6 now die. Second
  and worse: I had recorded, in the commit, the verification doc and the decision I
  put to Tim, that a one-point ghost was clearable only by *Wipe ink*. **False in
  Foundry.** `sheet.mjs` gives every `.cb-stroke` `tabindex="0"` while the pen is in
  hand and `#bindStrokeKeys` erases the focused one on Delete. Codex could not test
  whether a 0x0 element is really in the tab order and correctly refused to assert it;
  I measured it in a browser — one Tab lands on a one-point polyline announcing "Pen
  mark" and carrying its stroke id. So the ghost *was* selectively removable, and my
  renderer skip closes that path. The lesson is the plain one: I removed something
  before understanding why it was there, and stated the absence of a capability I had
  not checked for. Skip left standing pending Tim's call, since he chose it on my
  description. `ae1e5bc`, `aaf7ec1`.


- 2026-09-13 — **Review 27: the commit window.** Codex found that `#commit` built the
  undo's inverse before `await page.update()` and read the witness after, so any edit
  landing inside that await was witnessed as though it had always been there — the undo
  matched and restored a line built partly from a stranger's endpoint, collapsing it to
  length 0. `witnessAcross` takes each place from *after* when the payload wrote it and
  from *before* when it did not. Driven live on 14.363 by wrapping `page.update` to land
  the other user's write inside the update: the undo is refused, the line survives. This
  disproves review 25's "unreachable" verdict, and my 86,924-undo fuzz was blind to it
  **by construction** — it never modelled an interleaving inside the commit window. A
  fuzz proves nothing about a shape of event it cannot generate. Numbered 27 because a
  concurrent session had already committed its own review 26; I nearly overwrote it.
  `8d7cb0f`.

- **2026-09-13 — Corkboard: the renderer skip dropped, and what it cost to learn why.**
  Tim's call, taken again once the premise under it was corrected. The renderer no
  longer skips strokes it cannot paint; `isStroke` guards **creation only**, which was
  the whole of the original decision — no travel, no stroke. Everything else about
  `ab20565` stands. The general lesson, and it is the expensive one: **a stroke is an
  element before it is paint.** I reasoned about a one-point stroke entirely in terms
  of what it draws — nothing — and concluded it was unreachable, when it was reachable
  the whole time by Tab, carrying the id that erases it. Zero paint is not zero
  affordance. I then wrote that conclusion into a commit message, a verification doc
  and the options I put to Tim, so a false premise propagated into a decision before
  anyone tested it. What caught it was an external reviewer asking a question it could
  not answer — Codex flagged the tabindex and refused to assert the tab-order half,
  which is the honest shape for a finding that needs a browser. What settled it was
  ten lines of HTML in a real browser. Guard is now held by a test that states its own
  reason in place, so re-adding the skip fails loudly rather than reading as a tidy-up;
  11 of 11 mutants caught, including both forms of the regression. `80dc098`.

- **2026-09-13 — Corkboard review 28: no defect, and a comment I had half-corrected.**
  Codex cleared `80dc098` — the ink loop and `strokeElements` are byte-for-byte what
  they were before the episode began. Two P3s, both *around* the code. The comments
  one matters because a comment asserting an intent its code never carried out is what
  started all of this: my own correction fixed the paragraph I added and left the one
  above it standing, so `isStroke`'s doc opened by saying a ghost could only be cleared
  by Wipe ink and then explained two paragraphs later that Foundry's keyboard deletes
  it. It also claimed a right-click opens a mark's menu — it does not, `#bindPen` binds
  `contextmenu` straight to `#eraseStroke`. And `styles/app.css` still told the reader
  that selecting a mark with the pen leaves a dot every time, "on purpose" — the
  original rationale for a rule this work invalidated, and the one I would never have
  found by looking where I had been working. **Lesson: correcting a claim means finding
  every place it was stated, not the place you last wrote it.** The coverage P3: the
  drawn polyline's `cb-stroke` class is load-bearing — the sheet selects on it to
  assign tabindex — so a ghost could keep its id and label, lose that token, and be
  emitted yet unreachable; now asserted, 10 of 10 mutants caught. Two mutants remain in
  `sheet.mjs`, which imports Foundry and has no unit tests; recorded as owed a live
  check rather than papered over. Also: I wrote my review to `codex-review-27.md` and
  **overwrote the other session's file of that name** — restored from HEAD, nothing
  lost, renumbered to 28, and written into the review itself. Same concurrency hazard
  as the rest of the day in a new hat: not two writers in one file, two writers
  reaching for one NAME. `bd2517e`.


- **2026-09-13 — the twenty-first guard, built twice and folded into one.** Two sessions
  wrote the same guard within three minutes: `check-figures` reads measurement *phrasings*,
  `check-unmarked` read *structure* (bold in prose, column headers in tables). Each reported
  OK on what the other caught, so the disjointness was symmetric and neither was secondary.
  Folded into `check-figures` at `b946a28`, R-307. **The finding that made the boundary:**
  bold is a publication mark in prose and an emphasis mark in a table, and the corpus uses
  both correctly — so the bold reader stays silent inside tables. It also found a live defect
  first: a median of 24 rounds where the baseline holds 25, contradicting the same document's
  own correctly-cited 25 a hundred and thirty lines away.
- **2026-09-13 — `git add <file>` takes the whole file, and a green build can be green for
  another session's reasons.** A peer's commit carried eight hunks of my uncommitted work
  while staging two exact paths — the rule "never `git add -A`" is necessary and not
  sufficient. It took a document citing a key whose module it left behind, and their
  `npm run check` passed because my uncommitted file was in the shared tree: the commit was
  broken, the working copy was not. Two rules now: **`git diff --cached` before committing**,
  and prove the commit standalone in a clone. R-302, and in the memory file.

- 2026-09-13 — **The card editor's Text field took no keystrokes, and it was our CSS.**
  `.cb-edit-form prose-mirror` set `display: block`. Core lays that element out as a
  flex column whose `.editor-container` takes `flex: 1`, with the editable
  `.editor-content` inside it `position: absolute; inset: 0`. Forcing the host back to
  a block leaves the container with no in-flow child, so it collapses to zero height
  and takes the only typeable surface with it — 220px of paper and a working toolbar
  over nothing. Measured in a live Foundry 14.363 page: container and content 0px with
  the declaration, 160px without. jsdom has no layout engine, so **no test in the suite
  could ever have caught this**; the measurement lives in a comment above the rule.
  `4e1cc57`.
- 2026-09-13 — **Review 29: a rectangle's sides are not a dependency.** The witness
  widened every touched shape geometry key to `w`,`h`,`a`,`b`. Only `line` and `arrow`
  have a length no single field determines; a box is measured one side at a time, so
  widening there refused a safe undo (Alice undoing her own width while Bob's height
  stood). Gated on kind. The qualifier on the old lesson: couple fields that are
  *structurally* coupled, and let the schema say which — that is not a rule about
  values. One P2 left open on purpose: a write landing inside the undo's **own**
  update, which no pre-await check can close, because validation and application are
  not atomic. `9f72c5c`.
- **2026-09-13 — a guard that stopped another guard from running.** The fold exported
  `scanDocument` for the string tests, and importing it also ran the guard — so a figure
  defect called `process.exit(1)` inside `check-behaviour`, which reported check-figures'
  failure under its own name having run **zero of its 108 tests**. Not a check that passed
  for the wrong reason: a check that never ran and could not say so. Rule for the repo:
  **a module a guard imports must not be a guard that runs.** `d0db795`, R-309.
- 2026-09-13 — Corkboard phase 4 transfer done (4.1–4.3): `.corkboard` bundles, app export/import, and the Foundry path↔asset-id translation. First Foundry run of this work, which also cleared the `editCard`/`ingest` debt from slice 2. Six defects, all found by running it rather than by tests. 1242 tests, head `9dc9768`.

- **2026-09-13 — Corkboard: the ghost-stroke keyboard route, held live.** Review 28's
  owed check, run on foundry.oneoffgames.com as GM. Six cases pass: a 0x0 ghost is
  rendered with its id, gains `tabindex="0"` when the pen is picked up, takes focus
  from one Tab off the neighbouring stroke announcing "Pen mark", and Delete removes
  only its id — repeated on the highlighter layer. That holds the two `sheet.mjs`
  mutants no unit test can reach, because `sheet.mjs` imports Foundry. Tap-makes-no-
  stroke re-confirmed on the same board with a real drag as control. **Three process
  notes worth more than the result.** (1) `deploy.sh` was refused here as a production
  action; I did not work around it, went looking for a safer route, and found I needed
  none — a concurrent session had already shipped a5e772e carrying my commits, which I
  confirmed by md5 against a local build rather than taking on trust. Being blocked is
  a prompt to find a safer path, not a puzzle to solve. (2) The fixture was a throwaway
  board of its own, deleted after, with the four real boards counted before and after
  and unchanged — never verify on someone's live data when a scratch document costs
  nothing. (3) A ghost cannot be made through the UI any more, so legacy data had to be
  written straight into the document — and that it was accepted unchanged is itself the
  confirmation that `schema.mjs`/`validate.mjs` impose no minimum and real saved boards
  can hold these. Also today, a false alarm worth remembering: I grepped a committed
  file for a phrase that wraps across two lines, got zero, and briefly believed my own
  commit had lost work. `457dd2d`.

- 2026-09-13 — **A seed is only reproducible against a stated engine.** Loom's provenance
  recorded the seed but not the code that read it, so a row rewritten in a pack made old
  output unreproducible with no way to tell. `ENGINE` (`generators.mjs:52`, now 2) is written
  beside the seed and bumped whenever generation changes meaning. The Makers panel needed it
  added twice, because that view copies named fields onto its own result rows rather than
  passing the provenance through — a field added to the model does not appear there for free.
  `b6f7d2d`. [[loom]]
- 2026-09-13 — **The importer a running Loom holds is the one it booted with, and my own
  script hid it.** Five boards imported against three-commits-old code because the preview
  server predated the fixes; `tools/import-docs.mjs` reported the plan it had computed
  locally, which matched what I expected and not what the server stored (67 nodes/12 edges
  instead of 66/53, no PART cards). The script now prints the **server's** counts and warns
  on drift. A dry run is a prediction; only the response is evidence. `e32db38`. [[loom]]
- 2026-09-13 — **A guard in a closure inside a Foundry-importing module cannot be tested, so
  move the decision, not the code.** The rule that stops new-card marks being cleared while a
  gesture holds an element arrived with no test, because it lived in a private method of
  `sheet.mjs`, which has no coverage at all. It is now `shouldAcknowledge({fresh, active})` in
  `controller.mjs` — pure, Foundry-free, seven cases including the half that matters: deferring
  must not mean losing, so once the gesture ends the marks are still there to clear. Proved
  load-bearing rather than merely green — removing the `active` check fails exactly three.
  `9869072`. [[corkboard]]
- 2026-09-13 — **Two libWrapper OVERRIDEs on one method is a choice, not a bug to fix.**
  Zoom/Pan Options and EZGlide both claimed `Canvas.prototype._onMouseWheel`; their other hooks
  never collided. Zoom/Pan registers unconditionally, so the only fix keeping both was EZGlide's
  own smooth-zoom toggle — and that setting is **client**-scoped, so it would have fixed one
  browser and left every player the banner. Module enablement is world-scoped, so disabling
  EZGlide was the fix that reached everyone. Found two more worlds with the same pair by
  reading every world's `core.moduleConfiguration`; hand-scraping the LevelDB files misparsed
  most of them, and Foundry's own `classic-level` read all 42 cleanly. [[oneoffgames-vps]]
- 2026-09-13 — **The browser tool's console buffer spans a reload, so a stale error outlives
  the fix.** After disabling EZGlide the conflict line was still there and looked live. What
  gave it away was ordering: before the reload it read `ready → callback → conflict`, after it
  read `conflict → ready → callback`. Verify the page first (`performance.timeOrigin` against
  the deploy, or a sentinel on `window` that a real reload destroys), and prefer a mechanism
  check — a disabled module fetches no scripts, so it cannot register a wrapper at all.
  [[corkboard]], [[oneoffgames-vps]]
- 2026-09-13 — Corkboard 4.4: the app opens with the wifi off. Derived precache shell + generated service worker, update-on-consent, migration recovery for a cached shell meeting newer boards, installable with generated icons. Verified with the server killed and the port confirmed empty. 1292 tests, head `8e6c067`. iPad is now the phase-4 blocker.
- 2026-09-13 — **A double-fire report that was not one, and could not have been.** The board
  sheet's Undo button was reported firing its handler twice on a single click, producing two
  identical refusal toasts. It does not. Measured against the then-deployed build, which was
  byte-identical to `0abdd4d`: one real trusted mouse click gave one `undoLast`, one
  notification call and one DOM toast, and **every** `[data-action]` element in the sheet
  carried exactly one listener of each type across first render, plain re-render, two
  document-update renders, four view/edit mode switches, a close-and-reopen and an embedded
  render. Two listeners could not have produced the reported symptom in any build in the
  repo's history either, because both refusal branches set `#undo = null` synchronously
  *before* notifying — a second invocation in the same dispatch says "There is nothing to
  undo", never a second copy of the warning. The quoted message was itself unreachable:
  `undoConflict` returns only `tied`, `orphan` or `null`, so the `collapsed` string had been
  dead since the geometry guard was dropped. Nothing to fix; the dead string was removed
  separately in `87d6993`. The lesson is the symptom-to-mechanism check: the reported
  *symptom* was inconsistent with the proposed *cause* before any measurement, and noticing
  that first would have aimed the whole investigation. [[corkboard]]
- 2026-09-13 — **A session can park a Foundry world and then be unable to bring it back.**
  Game Settings → Return to Setup shuts the world down and lands on the admin-password gate,
  which a session will not type, and the server-side alternative (set `world` in
  `options.json`, restart `foundryvtt13`) is exactly what a permission classifier blocks. So
  a session can take production down and not put it up. `blade-runner` sat parked ~30 minutes
  before anyone noticed. The Foundry debug log records **no shutdown line at all** — only the
  next `Launching World | Complete` — so the log cannot tell you who did it or when; asking
  the other sessions is the only route, and it worked. [[oneoffgames-vps]]
- 2026-09-13 — **Four sessions deployed to the same live Foundry in one evening, one of them
  under a user mid-test.** `./deploy.sh --force` builds the *working tree* of a repo several
  sessions share, so "deploy" is never a private act: it publishes whatever is sitting there
  and swaps the module under anyone connected. Waiting for `git status` to go clean is
  necessary but not sufficient — the second half is knowing whether a human is on the server.
  Agreed convention: announce a deploy to Tim rather than just doing it, and check before the
  next one. A GM seat being occupied is **not** evidence the human is watching; it may be
  another session logged in as TimEvans to instrument something. [[corkboard]],
  [[oneoffgames-vps]]
- **2026-09-13** — Investigated the Blade Runner world found parked at `/setup`. It was
  **not** a crash, certbot, OOM or reboot: `foundryvtt13.service` has held the same PID
  since 11 Sep and the box is up 143 days. The world was parked by a deliberate
  `POST /setup` at **21:27:46 CEST** carrying a `/game` referer — a Return to Setup from
  inside the running game — and **relaunched by the same client at 21:34:39**, which
  rejoined at 21:34:55. All of it from `178.239.163.114`
  (`bandwidth.co.uk`, a UK ISP, macOS Chrome), authenticated as TimEvans at 20:42:44.
  No failed logins all day. The corkboard session's deploy landed at 22:06:52 and it
  joined at 22:08:32 from the home WAN — **32 minutes after the world was already back
  up**, so it did not relaunch anything. Open question for Tim: whether 178.239.163.114
  and 45.86.211.17 (which did the same join→setup→join cycle at 11:28) are him.
  [[oneoffgames-vps]] · [[corkboard]]
- 2026-09-13 — **Correction to tonight's world-park entry, on two counts.** (1) I wrote that
  `blade-runner` sat parked "~30 minutes"; the access log puts Return to Setup at 21:27:46 CEST
  and the relaunch at 21:34:39, so it was **under seven minutes**. (2) I wrote that the log
  "cannot tell you who took a world down". The *debug* log cannot — but the HTTP access log
  can, and timevans-42 found it: `POST /setup` carrying `Referer: .../game` is Return to Setup
  clicked from inside a running world, with a timestamp and a client IP. Use that, not
  interrogation, next time. [[oneoffgames-vps]]
- 2026-09-13 — **Two traps in reading a shared server's evidence, both of which produced a
  confident wrong answer tonight.** First: **a deployed file's mtime dates the most recent
  deploy, not yours.** `rsync` overwrites, so after another session deployed on top of mine the
  module's mtime read 22:06:52 and was taken as the moment *my* deploy landed — placing it 32
  minutes after an event it actually preceded by a minute. The content settles it where the
  timestamp cannot: the live `sheet.mjs` hashed to commit `7330641`, four commits past the
  `a5e772e` I shipped. **Hash the file, do not trust its mtime.** Second: **one client IP is not
  one actor.** Several sessions drive Tim's Chrome through the same extension, so their browsing
  all leaves from one address while their ssh leaves from another — which is why one session's
  Return to Setup and another's relaunch appear on a single IP seven minutes apart. Read that as
  one admin and you invent an intruder. Both errors were caught by reconstructing from session
  transcripts, which is the evidence a shared server's logs cannot supply.
  [[oneoffgames-vps]], [[corkboard]]
- **2026-09-13 — correction to the entry above.** The claim that the corkboard session
  deployed at 22:06:52 was **wrong, and the reasoning was unsound**. A deployed file's
  mtime dates the most recent deploy, not any particular one, and `build.mjs` recursively
  copies `src/`, so every deploy rewrites all 38 files with that moment's timestamp and
  erases the evidence of earlier ones. Verified properly by content instead: the live
  `src/board/sheet.mjs` is md5 `7c028050…`, which matches **the working tree and no commit
  at all** — not `a5e772e` (`48f9fe9a…`), not `7330641` (`b49f3dce…`), not HEAD
  (`4f8ac9c6…`). So the 22:06:52 deploy shipped *uncommitted* state and was not that
  session's. The "32-minute gap" it was accused of does not exist. Correction raised by
  that session; its own md5 figures did not reproduce, but its conclusion was right and
  mine was not. **Lesson: verify a deploy by file content against commits, never by mtime.**
  [[corkboard]] · [[oneoffgames-vps]]
- **2026-09-13** — Still open, and not retracted: whether `178.239.163.114` is Tim. The
  peer's explanation is that several sessions drive Tim's own Chrome through the
  Claude-in-Chrome extension, so their ssh exits the home WAN while their browsing exits
  elsewhere. Checked what could be checked: exactly one Chrome is connected to the
  account, it is on this Mac, and this Mac's default route is `en1` → `192.168.0.1`, so
  its browser exits `185.66.205.125` like its ssh does. NordVPN and Shadowrocket are
  installed but disconnected — either could have been up earlier, which would explain it.
  `178.239.163.114` reverses to a UK consumer ISP, not a datacenter, so it looks like a
  domestic line rather than VPN infrastructure. **Unresolved: possible but unverified.**
  A second outside IP, `45.86.211.17`, did the same join→setup→join cycle at 11:28 and
  01:01 and is outside every session's account of tonight. Both still need Tim's yes or no.
  [[oneoffgames-vps]]
- 2026-09-13 — **scenario-forge: the house scenario format becomes a compiler.** Six
  scenarios, three spellings of the same heading, a Revelations checklist in three of
  six — a style guide followed by eye drifts, so it gets executed instead. Markdown +
  YAML front-matter + a thin `scenario.config.mjs` build a Foundry module and four
  [[corkboard]] boards generated from tables the scenarios already contain (Clue Trail,
  NPC Roster, Countdown). [[afterimage]]'s build.mjs is the seed — 211 lines, 8 of which
  mention AFTERIMAGE. Spec committed `e405c38`, no code yet, no remote yet.
  [[scenario-forge]]
- 2026-09-13 — **Spike: a `corkboard.board` page survives packing and Adventure import
  intact** — type, `system` and `flags` all deep-equal after a LevelDB round trip, and
  Foundry 14.361's `TypeDataField.initialize` returns `deepClone(value)` when no data
  model is registered, so a board survives even with Corkboard **disabled** (the unknown
  type passes because `fallback` defaults to `!strict`, `common/abstract/data.mjs:79`).
  Packing was never the risk: it is JSON into LevelDB with no validation layer, so it
  cannot strip anything. Boards therefore ship inside the Adventure pack, and the
  `.corkboard` bundle is a standalone-app feature rather than a fallback.
  [[scenario-forge]] · [[corkboard]]
- 2026-09-13 — **Adventure re-import is destructive, and that changed the design.**
  `Adventure#prepareImport` partitions on `collection.has(d._id)` and sends existing
  documents to `updateDocuments` with `{diff: false, recursive: false}` — so with
  deterministic ids, re-importing replaces a board page **wholesale**: every card the GM
  moved, every card they added, gone, with no diff and no prompt. Corkboard re-sync
  (replace `sf-` entities, keep their positions, never touch anything else) moved from
  phase 2 to phase 1 as a result: it is the only non-destructive path from an edited
  scenario to an updated board. [[scenario-forge]] · [[corkboard]]
- 2026-09-13 — **The Foundry CLI is not in this pipeline, deliberately.** AFTERIMAGE
  writes LevelDB directly because the CLI splits a scene's `levels` into their own
  sublevel keys and v14 fails to reassemble them — it synthesises a blank level and the
  background art is lost. Carried into scenario-forge's packer *with the comment*, or
  somebody tidies it back to the CLI within a year. [[scenario-forge]] · [[afterimage]]
- 2026-09-13 — Corkboard 4.5 done, phase 4 complete. Touch work was largely already built in phase 3 and had never run under a finger; now verified live (pinch ratio exact, stray-thumb cancel writes nothing, hit targets 45-47px). Added iOS callout + tap-highlight suppression and a toolbar scroll-shadow. 1292 tests, head `b0456c7`. Only the iPad is outstanding.
- **2026-09-13 — the outside-admin question is CLOSED, and it was never an intruder.**
  Measured rather than argued: fetching `api.ipify.org` through the Claude-in-Chrome
  browser tool returns `{"ip":"178.239.163.114"}`. So that address is the egress of the
  browser extension itself, not a foreign network — the sessions that parked and
  relaunched the world were driving Tim's own Chrome. My contrary inference (one Chrome,
  `isLocal` true, this Mac's default route is `en1`, therefore its browsing exits
  `185.66.205.125`) was a reasonable test that simply does not describe the path the
  extension takes: **the extension's traffic does not follow the host's default route,
  so the host's egress tells you nothing about the browser's.** No action needed from
  Tim. `45.86.211.17` is unexplained only in the sense that nobody has claimed it; the
  same mechanism plausibly covers it. [[oneoffgames-vps]]
- **2026-09-13 — correction: the 22:06:52 deploy did NOT ship uncommitted state.** That
  claim of mine was an artefact of a broken hashing method. Resolved properly with
  `git ls-tree -r <ref> -- <path>` then `git cat-file blob <sha>`: `sheet.mjs` is blob
  `69358f33`, 116483 bytes, md5 `7c028050…` in `7330641`, in `b0456c7`, in HEAD **and**
  in the working tree — the file simply has not changed since `7330641`. "Live matches the
  tree" and "live matches a commit" were both true at once. There was no dirty deploy.
  [[corkboard]]
- **2026-09-13 — gotcha worth keeping: `git show <ref>:<path>` returned the wrong bytes.**
  On git 2.54.0 with no `.gitattributes`, `git show a5e772e:src/board/sheet.mjs | wc -c`
  gave 99321 bytes against a real blob of 107404, and 27669 against 116483 for `7330641`.
  The md5s taken from it looked perfectly plausible and were meaningless. Cause not
  established. **Use `git ls-tree` to get the blob sha, then `git cat-file blob` to read
  it, and check the byte count against `git cat-file -s` before trusting any hash.**
  A hash is only as good as the object you proved you hashed. [[CONVENTIONS]]
- 2026-09-13 — scenario-forge has a home: private GitHub repo `slaguru666/scenario-forge`,
  spec pushed at `e405c38`. `git init` defaulted to `master`; renamed to `main` before the
  first push to match every other repo. The GitHub MCP was unauthenticated this session, so
  the repo was created with `gh`. [[scenario-forge]]
- **2026-09-13 — the bad-hash mystery is solved, and it was zsh, not git.** Reproduced
  here: in zsh, `"$ref:src/board/sheet.mjs"` expands to bare `a5e772e`, because `:s` is
  read as a history-substitution modifier on the parameter and the rest is consumed
  silently. So `git show` and `git cat-file -p` were each handed a commit with no path
  and returned the commit's own bytes — 99321 for `git show`, 2652 for `cat-file -p`,
  both hashing to plausible nonsense. Braced, `"${ref}:..."` returns the true blobs at
  107404 and 116483 bytes. My earlier entry blaming `git show` was wrong; git behaved
  correctly throughout. Diagnosis from the corkboard session, verified independently
  here. The rule in [[CONVENTIONS]] is now the sharper one: **brace the variable**.
- **2026-09-13** — Fixed the `ringworld` system on the VPS. It declared two files that
  had never existed — `styles/ringworld.css` and `lang/en.json` — so every Setup scan
  logged a metadata validation error. Created both (the CSS is an empty commented stub,
  the lang file is `{}`); `system.json` untouched. Note it is a **v0.1.0 stub from
  December 2025 with zero templates, zero esmodules, zero packs, used by none of the 49
  worlds** — the playable system is `ringbrp` v1.6.0 (The Custodians). Deleting the stub
  would be the cleaner end-state; left in place because that was not asked for.
  [[oneoffgames-vps]] · [[custodians-ringbrp]]
- **2026-09-13** — `Projects/oneoffgames-vps.md` was found carrying **two `**Gotchas**`
  sections**, the whole block duplicated with three bullets stranded after the `Related:`
  footer. It had been that way since `c08325b` and grew across five of my commits
  (7121 → 9110 bytes) because I kept anchor-replacing into an already-doubled file.
  Merged back to one section by the corkboard session in `a558693`, down to 7288 bytes.
  Swept the rest of the vault afterwards: no other note is doubled, and the 45 log
  entries carry no duplicate bullets. **The vault has the same shared-tree hazard as the
  repo, and it is worse here because nothing complains** — rules now in [[CONVENTIONS]]
  under *Writing to the vault*, limitation recorded in [[DESIGN]].
- **2026-09-13** — Built the `sync.sh` vault guard, on Tim's direct approval (a peer
  relayed the approval; relayed approval is not approval, so it was confirmed with him
  first). It runs against **canonical, before the rsync**, so a doubled note never
  reaches the repo — checking before the *push* would be too late, since the repo copy
  would already hold it. Duplicated headings skip the vault stage only, leaving
  `CLAUDE.md`, settings, memory and skills to sync normally, and the run exits 1 naming
  the file and the heading. It also warns when a note exists in `claude-config/vault/`
  but not canonical, which is the silent-deletion case. Escape hatch:
  `--allow-duplicate-headings`. Verified live in all three modes — clean run passes,
  an injected `**Gotchas**` duplicate is refused with the note held out of the repo,
  and the flag parses without eating the commit message. One bug the scratch test missed
  and the live script would have hit: `sync.sh` runs under `set -euo pipefail`, so a
  `grep` with no matches aborts the script — every grep in the guard now ends `|| true`.
  [[claude-config-sync]]
